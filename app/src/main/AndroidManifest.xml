<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.DUMP" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SystemDebugger"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".AuthActivity"
            android:exported="true"
            android:theme="@style/Theme.SystemDebugger">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.SystemDebugger" />

        <activity
            android:name=".feature.AppFreezerActivity"
            android:exported="false"
            android:theme="@style/Theme.SystemDebugger" />

        <activity
            android:name=".feature.PrivacyShieldActivity"
            android:exported="false"
            android:theme="@style/Theme.SystemDebugger" />

        <activity
            android:name=".feature.FileExplorerActivity"
            android:exported="false"
            android:theme="@style/Theme.SystemDebugger" />

        <activity
            android:name=".feature.SystemTunerActivity"
            android:exported="false"
            android:theme="@style/Theme.SystemDebugger" />

        <activity
            android:name=".feature.SecurityAuditActivity"
            android:exported="false"
            android:theme="@style/Theme.SystemDebugger" />

        <activity
            android:name=".feature.AutomationSceneActivity"
            android:exported="false"
            android:theme="@style/Theme.SystemDebugger" />

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

    </application>

</manifest>